package com.echo.scala

object Lesson_String {
  def main(args: Array[String]): Unit = {
    val s = "echo"
    val s1 = "ECHO"

//    println(s.indexOf(99))//返回ascll码98所对应的字符在字符串中第一次出现的位置
//    println(s.indexOf(98))//不存在就返回-1

//    println(s.equals(s1))//比对
//    println(s.equalsIgnoreCase(s1))//字符串比对忽略大小写
  }
}
